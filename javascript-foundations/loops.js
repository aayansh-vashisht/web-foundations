// Classic 'for' loop (when you know the repetition count)
for (let i = 1; i <= 3; i++) {
  console.log(`Step ${i}`);
}

// 'for...of' loop (best for looping through arrays)
const colors = ["red", "green", "blue"];
for (const color of colors) {
  console.log(color);
}