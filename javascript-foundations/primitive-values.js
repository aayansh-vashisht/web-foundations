const text = "Hello world"; // String
const count = 42.9;           // Number
const isActive = true;      // Boolean (true or false)
const empty = null;         // Explicitly "nothing"
let notDefined;             // Undefined (declared, but no value set)

console.log(count);