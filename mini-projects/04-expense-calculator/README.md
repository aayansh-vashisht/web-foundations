# Expense Calculator

An expense tracking interface that aggregates numbers and displays dynamic totals.

### Core Concepts Practiced
- Form handling and `event.preventDefault()`
- Array methods (`reduce`, `forEach`, `splice`)
- Number parsing and precision (`parseFloat`, `.toFixed()`)