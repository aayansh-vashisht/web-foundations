# Searchable List

A real-time search interface filtering an array in memory.

### Core Concepts Practiced
- `input` event detection
- Case-insensitive string matching (`.toLowerCase()`, `.includes()`)
- Array filtering using `Array.prototype.filter`