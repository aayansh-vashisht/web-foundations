# Counter App

A minimal counter application built with vanilla JavaScript.

### Core Concepts Practiced
- Variable state management (`let`)
- DOM selection (`document.querySelector`)
- Event listeners (`click`)
- Conditional UI styling