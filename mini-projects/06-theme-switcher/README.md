# Theme Switcher

A dark/light mode toggle that saves user preferences to browser storage.

### Core Concepts Practiced
- CSS custom properties (variables)
- Class list toggling (`classList.toggle`, `classList.contains`)
- `localStorage.setItem` and `localStorage.getItem`