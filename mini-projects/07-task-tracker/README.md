# Task Tracker (Local Storage)

A complete CRUD task management app with persistent local storage.

### Core Concepts Practiced
- `JSON.stringify()` and `JSON.parse()`
- Persisting complex data arrays in `localStorage`
- Object state toggling and array mutation (`splice`)